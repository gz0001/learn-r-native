import { StyleSheet } from "react-native";

export default (styles = StyleSheet.create({
  listContainer: {
    width: "80%",
    height: "70%",
    backgroundColor: "#42bff4",
    borderWidth: 1,
    borderColor: "grey"
  }
}));

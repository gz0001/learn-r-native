import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    minHeight: "100%",
    paddingVertical: 50,
    alignItems: "center"
  },
  box: {
    width: 100,
    height: 100,
    backgroundColor: "red"
  }
});

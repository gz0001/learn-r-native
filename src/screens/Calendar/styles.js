import { StyleSheet } from "react-native";

export default StyleSheet.create({
  iconView: {
    position: "absolute",
    bottom: 30,
    right: 30,
    justifyContent: "center",
    alignItems: "center",
    width: 60,
    height: 60,
    borderRadius: 30,
    backgroundColor: "red",
    zIndex: 100
  }
});

const base = `http://10.0.2.2:3333/api`;

export default {
  categories: `${base}/categories`,
  posts: `${base}/posts`,
  statuses: `${base}/statuses`,
  tags: `${base}/tags`,
  users: `${base}/users`
};
